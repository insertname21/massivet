version: 1
frontend:
  phases:
    

    build:
      commands: 
        - react-scripts build

  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: 
        - node_modules/**/*
